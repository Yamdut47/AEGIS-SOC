const firebaseConfig = {
  apiKey: "PASTE_YOUR_API_KEY",
  authDomain: "aegis-soc-6e564.firebaseapp.com",
  databaseURL: "https://aegis-soc-6e564-default-rtdb.firebaseio.com",
  projectId: "aegis-soc-6e564",
  storageBucket: "aegis-soc-6e564.appspot.com",
  messagingSenderId: "PASTE_YOUR_SENDER_ID",
  appId: "PASTE_YOUR_APP_ID"
};

firebase.initializeApp(firebaseConfig);
const auth = firebase.auth();
// Safe: pages that don't load the database script won't crash
const db = (typeof firebase.database === "function") ? firebase.database() : null;